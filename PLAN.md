# خطة تطوير منصة معاذ عليان (Enterprise Laravel 13)
هذه الخطة مقسمة إلى مراحل تنفيذية (Checklists). كل نقطة يتم إنجازها واعتمادها سيتم وضع علامة [x] عليها قبل الانتقال للتي تليها.

---

## [x] المرحلة الأولى: التأسيس والبنية التحتية (Foundation)
- [x] **تثبيت الحزم الأساسية:** تثبيت وإعداد `mcamara/laravel-localization` للروابط، `spatie/laravel-translatable` لقاعدة البيانات، و `spatie/laravel-permission` للصلاحيات.
- [x] **إعداد الحمايات (Guards):** إنشاء وتفعيل الـ Multi-Guard (`admin` للداشبورد و `web` للموقع) في `config/auth.php`.
- [x] **إعداد الأداء (Performance):** تهيئة اتصال `Redis` للـ Cache والـ Queues، وتفعيل `Model::preventLazyLoading()` لمنع مشاكل N+1 بشكل قطعي.

## [x] المرحلة الثانية: قاعدة البيانات والنماذج (Database & Models)
- [x] **تهيئة الجداول (Migrations):** إنشاء جداول النظام بالكامل (`admins`, `users`, `settings`, `courses`, `articles`... إلخ) مع تطبيق الـ JSON columns للحقول متعددة اللغات، والـ Indexes اللازمة.
- [x] **إنشاء الـ Enums:** تجهيز فئات `VideoCategory`, `ArticleCategory`, `BookCategory` لتقييد أنواع البيانات.
- [x] **إنشاء الـ Models:** ربط العلاقات (Relationships)، وتفعيل خيارات الترجمة (Translatable)، وكتابة الـ Local Scopes لتسهيل الفلترة.

## [x] المرحلة الثالثة: طبقة مستودع البيانات (Repository Layer)
- [x] **إنشاء الـ Interfaces:** تحديد عقود العمل لكل Model لضمان مبدأ (Open/Closed).
- [x] **بناء الـ Implementations:** كتابة الكود الفعلي لجلب البيانات مع تطبيق الـ Eager Loading والـ Pagination.
- [x] **الربط (Binding):** تجهيز `RepositoryServiceProvider` لربط الـ Interfaces بالـ Implementations الخاصة بها.

## [x] المرحلة الرابعة: طبقة الخدمات ونقل البيانات (Services & DTOs)
- [x] **إنشاء الـ DTOs:** بناء كائنات نقل البيانات الموثوقة مثل `CommentDTO` و `ContactDTO`.
- [x] **خدمات النظام الأساسية (Core Services):**
  - [x] برمجة `SettingService` لإدارة الإعدادات الثابتة (مع تفعيل Redis Cache).
  - [x] برمجة `SeoService` لتوليد الميتا تاج والـ Schema.org لكل لغة.
- [x] **خدمات الداشبورد (Dashboard Services):** بناء الـ Business Logic الخاص بإنشاء وتعديل البيانات من قبل الإدارة.
- [x] **خدمات الموقع (Frontend Services):** بناء خدمات جلب البيانات للعرض (مثل `HomeService`) وتفعيل التخزين المؤقت (Caching) للبيانات الثقيلة.

## [x] المرحلة الخامسة: لوحة التحكم (Dashboard Development)
- [x] **هيكل الداشبورد:** تركيب الثيم المخصص، وإعداد الـ Layout العام للوحة التحكم (Sidebar, Header).
- [x] **نظام الدخول والصلاحيات:** برمجة شاشة الدخول للداشبورد `AdminAuthController`، وإضافة لوحة تحكم `Roles & Permissions`.
- [x] **إدارة الإعدادات الثابتة:** برمجة صفحة التحكم بالإعدادات (اللغة، الدارك مود، السوشيال ميديا).
- [x] **إدارة المحتوى (CRUDs):** إنشاء شاشات إدارة (الكورسات، المقالات، الفيديوهات، إلخ) مع دعم نظام إدخال البيانات المترجمة (Tabs للعربي والإنجليزي).

## [x] المرحلة السادسة: الموقع الأمامي (Frontend Development)
- [x] **الروابط واللغات:** إعداد مسارات `Mcamara` (Routes) للموقع لتدعم `/ar/` و `/en/`.
- [x] **تطوير الصفحات الأساسية:** تركيب واجهات الموقع (الرئيسية، الكورسات، المقالات) وربطها بالـ Controllers الخاصة بها.
- [x] **تفعيل الـ SEO والتجاوب:** تطبيق `x-seo-head` في كافة الصفحات، وضمان تناسق الدارك/لايت مود بناءً على إعدادات الـ Settings أو تفضيل المستخدم.
- [x] **منطقة المستخدم:** برمجة شاشة تسجيل دخول/إنشاء حساب الزوار، وبرمجة الملف الشخصي (Profile).

## [x] المرحلة السابعة: البيانات التجريبية والاختبار (Seeding & QA)
- [x] **الـ Seeders:** كتابة Seeders لضخ بيانات حقيقية تتطابق مع محتوى "معاذ عليان" الفعلي (كورسات، مناظرات مشهورة، مقالات).
- [x] **مراجعة الأداء:** مراجعة عدد الاستعلامات عبر Laravel Debugbar، والتأكد من خلو النظام من أي عنق زجاجة (Bottleneck).

---

### قواعد العمل المعمارية والأمنية (Architecture & Security Rules):
1. **الـ Controllers نظيفة تماماً:** لا يُسمح بكتابة أي Logic فيها. يتم الاعتماد على Services و Repositories عبر الـ Dependency Injection حصراً.
2. **نظام الفلترة (Pipeline Pattern):** كافة عمليات الفلترة المعقدة والبحث (Filters) يجب أن تمر عبر الـ `Pipeline` الخاص بلارافيل، ولا يُسمح باستخدام جُمل `if` متداخلة في الـ Repositories.
3. **التحقق والأمان الصارم (Strict Validation):** يمنع منعاً باتاً استقبال أي بيانات من المستخدم (User) أو الإدارة (Admin) بدون مرورها على `FormRequest` مخصص لعمل Validation دقيق لحماية النظام من XSS و SQL Injection وأي ثغرات أخرى.
4. التحديث المباشر لحالة الخطة (تغيير `[ ]` إلى `[x]`) في هذا الملف فور الانتهاء من أي جزء.
